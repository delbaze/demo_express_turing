import express from "express";
import skillsController from "../controller/skills";

const router = express.Router();

// router.route("/").post(skillsController.create)
// .get(skillsController.read);
// router
  // .route("/skills/:id")
  // .patch(skillsController.update)
  // .delete(skillsController.delete);

export default router;
